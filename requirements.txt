# Core optimization
ortools>=9.10

# Scientific Python
numpy>=1.26
# Pin pandas to 2.2.x: pandas 3.x hard-requires pyarrow, whose bundled abseil
# collides with OR-Tools' abseil at runtime on macOS and deadlocks CP-SAT.
pandas>=2.1,<2.3
pydantic>=2.6

# Visualization
matplotlib>=3.8

# UI (optional but used by app/streamlit_app.py)
streamlit>=1.32

# Live solver viewer (optional, used by src/app/live_server.py)
flask>=3.0

# Data ingestion (Penn Dining parser)
requests>=2.31
beautifulsoup4>=4.12

# Testing
pytest>=8.0
